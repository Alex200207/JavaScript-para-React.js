//Arrays (arreglos)
// Un arreglo es una estructura de datos que nos permite almacenar varios valores en una sola variable.
// Un arreglo puede contener cualquier tipo de dato, incluso otros arreglos.
//estos solo tiene un indice que empieza en 0 y un valor
//en lugar de llaves se usan corchetes

const tegnologias = ['HTML', 'CSS', 'JavaScript','React',true,2002];//un arreglo es una coleccion de elementos que se encuentran en una sola variable
console.table(tegnologias);//para acceder a los elementos de un arreglo se hace con la sintaxis de corchetes

console.log(tegnologias[0]);//acceder al primer elemento de un arreglo
